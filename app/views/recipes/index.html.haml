%h1 My Recipez

%table{:class => "table table-hover"}
	%tr
		%th Title
		%th Description
		%th
	- @recipes.each do |recipe|
		%tr
			%td= recipe.title
			%td= recipe.desc
			%td
				= link_to "Edit", edit_recipe_path(recipe), class: "btn btn-primary" 
%hr
= paginate @recipes

%br 
= link_to "Create New Recipez", new_recipe_path, class: 'btn btn-primary' 				