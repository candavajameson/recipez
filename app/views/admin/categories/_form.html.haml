= form_form @category, html: { class: 'form-horizontal'} do |f|
	.row
		.col-md-12
			.form-group
				= f.label :name, class: 'control-label'
				.col-md-8
					= f.text_field :name, class: 'form-control'		
		.form-actions
			= f.submit "Save Category", class: 'btn btn-primary'